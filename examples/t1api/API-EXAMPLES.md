# HyperNix T1 API — example requests and responses

**Generated by `scripts/t1api_examples.py`. Do not edit by hand.**

Every exchange below was recorded from a real server rather than
written out, so these are a recording of the API's behaviour and not
a claim about it. Re-run the script after changing a response shape
and the diff shows exactly what a client would see differently.

Volatile values (ids, timestamps, request ids) are replaced with
`<placeholder>` so regenerating produces a reviewable diff instead of
a wall of new uuids. Keys and tokens are placeholders throughout —
no real credential is ever recorded here.

The models shown are the shipped **example registry** entries, which
the spec describes as placeholders that are not real models. A live
deployment points `T1_MODEL_REGISTRY_PATH` at its own data.

## Contents

- [Health and status](#health-and-status)
- [Authentication](#authentication)
- [Models](#models)
- [Usage and cost](#usage-and-cost)
- [Keys](#keys)
- [Servers](#servers)
- [Modules](#modules)
- [Jobs and events](#jobs-and-events)
- [Billing](#billing)
- [Security and audit](#security-and-audit)

## Health and status

### `GET /health`

No auth. Touches no database — safe for a load balancer.

Response `200`:

```json
{
  "status": "ok",
  "request_id": "<request_id>"
}
```

### `GET /status`

Which protections are on, and which secrets are set (never their values).

Response `200`:

```json
{
  "status": "ok",
  "environment": "development",
  "t1_api_version": "<t1_api_version>",
  "hypernix_version": "<hypernix_version>",
  "beta": "beta3",
  "model_count": 9,
  "storage_backend": "sqlite",
  "request_id": "<request_id>",
  "tls_enabled": false,
  "mtls_mode": "off",
  "rate_limit_enabled": true,
  "audit_enabled": true,
  "network_policy_enabled": true,
  "allow_unlisted_clients": true,
  "remote_deployment_enabled": true,
  "secrets_configured": {
    "token_secret": true,
    "deploy_secret": true,
    "tls_keyfile": false,
    "tls_ca_certs": false,
    "database_url": false
  },
  "production_ready": false,
  "production_warnings": [
    "T1_DATABASE_URL is not set. SQLite is the documented development backend; production deployments should use PostgreSQL.",
    "No TLS configured (T1_TLS_CERTFILE/T1_TLS_KEYFILE) and not marked as running behind a TLS-terminating proxy (T1_MTLS_BEHIND_PROXY). Credentials would cross the network in plaintext.",
    "T1_EXPOSE_DOCS=1 serves Swagger/ReDoc publicly. Set T1_EXPOSE_DOCS=0 if the OpenAPI surface should not be reachable in production."
  ]
}
```

### `GET /config`

An explicit allowlist of settings. A secret cannot leak here by being forgotten.

Response `200`:

```json
{
  "config": {
    "environment": "development",
    "mount_prefix": "",
    "storage_backend": "sqlite",
    "enable_example_models": false,
    "routing_policy_path": null,
    "default_plan": "free",
    "usage_reset_period_seconds": 86400.0,
    "scoped_token_default_ttl_seconds": 3600,
    "rate_limit_enabled": true,
    "audit_enabled": true,
    "network_policy_enabled": true,
    "allow_unlisted_clients": true,
    "require_destructive_confirmation": true,
    "remote_deployment_enabled": true,
    "max_transfer_bytes": 268435456,
    "tls_enabled": false,
    "mtls_enabled": false,
    "mtls_mode": "off",
    "client_allowlist_configured": false
  },
  "request_id": "<request_id>"
}
```

## Authentication

### `POST /auth/t1/validate`

Validate a raw key. The response never echoes the key back.

Request:

```json
{
  "key": "<key>"
}
```

Response `401`:

```json
{
  "error": {
    "code": "AUTH_INVALID_KEY",
    "message": "Not a valid T1 key format: <T1_KEY>\u2026",
    "details": {}
  },
  "request_id": "<request_id>"
}
```

### `POST /auth/token`

Exchange a key for a short-lived scoped token. A token can only narrow the key's scopes, never widen them.

Request:

```json
{
  "key": "<key>",
  "scopes": [
    "read"
  ],
  "ttl_seconds": 3600
}
```

Response `401`:

```json
{
  "error": {
    "code": "AUTH_INVALID_KEY",
    "message": "Not a valid T1 key format: <T1_KEY>\u2026",
    "details": {}
  },
  "request_id": "<request_id>"
}
```

### `POST /auth/t1/rotate` *(authenticated)*

Rotate your own credential. The only endpoint that returns a raw key, because it mints a new one — the old key stops working immediately.

Response `200`:

```json
{
  "key_id": "<key_id>",
  "key": "<key>",
  "key_type": "user",
  "scopes": [
    "read"
  ],
  "rotated_from": "<rotated_from>",
  "request_id": "<request_id>"
}
```

### `POST /auth/t1/admin/rotate` *(authenticated)*

Admin-only. Shown here refused, because promoting a key is exactly the operation a non-admin must not reach.

Request:

```json
{
  "target_key_id": "<TARGET_KEY_ID>",
  "promote_to_admin": true
}
```

Response `403`:

```json
{
  "error": {
    "code": "AUTH_ADMIN_REQUIRED",
    "message": "This operation requires an admin-scoped T1 key.",
    "details": {}
  },
  "request_id": "<request_id>"
}
```

## Models

### `GET /models`

The registry is the only source of models. Nothing else can name one.

Response `200`:

```json
{
  "models": [
    {
      "model_id": "ryiver-1",
      "display_name": "Ryiver 1",
      "version": "1.0",
      "architecture": "moe",
      "status": "available",
      "availability": "restricted",
      "minimum_plan": "enterprise",
      "free_tier_available": false,
      "routing_priority": 5,
      "context_limit": 256000,
      "input_token_limit": 256000,
      "output_token_limit": 16384,
      "tool_call_limit": 128
    },
    {
      "model_id": "hypernix-1",
      "display_name": "HyperNix 1",
      "version": "1.0",
      "architecture": "moe",
      "status": "available",
      "availability": "restricted",
      "minimum_plan": "pro",
      "free_tier_available": false,
      "routing_priority": 10,
      "context_limit": 128000,
      "input_token_limit": 128000,
      "output_token_limit": 8192,
      "tool_call_limit": 64
    },
    {
      "model_id": "nanonix-mini-plus",
      "display_name": "nanoNix-mini-plus",
      "version": "1.0",
      "architecture": "moe",
      "status": "available",
      "availability": "public",
      "minimum_plan": "free",
      "free_tier_available": true,
      "routing_priority": 15,
      "context_limit": 32768,
      "input_token_limit": 32768,
      "output_token_limit": 4096,
      "tool_call_limit": 16
    },
    {
      "model_id": "nanonix",
      "display_name": "nanoNix",
      "version": "1.0",
      "architecture": "moe",
      "status": "available",
      "availability": "restricted",
      "minimum_plan": "pro",
      "free_tier_available": false,
      "routing_priority": 20,
      "context_limit": 65536,
      "input_token_limit": 65536,
      "output_token_limit": 8192,
      "tool_call_limit": 32
    },
    {
      "model_id": "nanonix-mini",
      "display_name": "nanoNix-mini",
      "version": "1.0",
      "architecture": "moe",
      "status": "available",
      "availability": "public",
      "minimum_plan": "free",
      "free_tier_available": true,
      "routing_priority": 25,
      "context_limit": 32768,
      "input_token_limit": 32768,
      "output_token_limit": 4096,
      "tool_call_limit": 16
    },
    {
      "model_id": "nanonanonano-n3",
      "display_name": "nanoNanonano / N^3",
      "version": "1.0",
      "architecture": "moe",
      "status": "available",
      "availability": "public",
      "minimum_plan": "free",
      "free_tier_available": true,
      "routing_priority": 30,
      "context_limit": 32768,
      "input_token_limit": 5000,
      "output_token_limit": 4096,
      "tool_call_limit": 16
    },
    {
      "model_id": "nanonix-mini-lite",
      "display_name": "nanoNix-miniLite",
      "version": "1.0",
      "architecture": "moe",
      "status": "available",
      "availability": "public",
      "minimum_plan": "free",
      "free_tier_available": true,
      "routing_priority": 35,
      "context_limit": 16384,
      "input_token_limit": 16384,
      "output_token_limit": 2048,
      "tool_call_limit": 8
    },
    {
      "model_id": "nanonix-nano",
      "display_name": "nanoNix-nano",
      "version": "1.0",
      "architecture": "dense",
      "status": "available",
      "availability": "public",
      "minimum_plan": "free",
      "free_tier_available": true,
      "routing_priority": 40,
      "context_limit": 8192,
      "input_token_limit": 8192,
      "output_token_limit": 2048,
      "tool_call_limit": 4
    },
    {
      "model_id": "nanonanonanonano-n4",
      "display_name": "nanoNanonanonano / N^4",
      "version": "1.0",
      "architecture": "dense",
      "status": "available",
      "availability": "public",
      "minimum_plan": "free",
      "free_tier_available": true,
      "routing_priority": 50,
      "context_limit": 4096,
      "input_token_limit": 4096,
      "output_token_limit": 1024,
      "tool_call_limit": 2
    }
  ],
  "count": 9,
  "request_id": "<request_id>"
}
```

### `GET /models/nanonix-nano`

Response `200`:

```json
{
  "model": {
    "model_id": "nanonix-nano",
    "display_name": "nanoNix-nano",
    "version": "1.0",
    "architecture": "dense",
    "status": "available",
    "availability": "public",
    "minimum_plan": "free",
    "free_tier_available": true,
    "routing_priority": 40,
    "context_limit": 8192,
    "input_token_limit": 8192,
    "output_token_limit": 2048,
    "tool_call_limit": 4,
    "total_parameters": 7.1,
    "active_parameters": null,
    "supported_tasks": [
      "chat",
      "completion"
    ],
    "api_available": true,
    "local_available": true,
    "remote_available": true,
    "pricing": {
      "input_price_per_1k": 0.0,
      "output_price_per_1k": 0.0,
      "currency": "USD"
    },
    "fallback_model": "nanonanonanonano-n4",
    "license": "unspecified",
    "is_example_entry": false,
    "notes": "Low-cost fallback per the spec's free-tier routing example."
  },
  "request_id": "<request_id>"
}
```

### `GET /models/does-not-exist`

Unregistered ids always return MODEL_NOT_SUPPORTED — never a fall-through to a client-supplied path.

Response `404`:

```json
{
  "error": {
    "code": "MODEL_NOT_SUPPORTED",
    "message": "Model 'does-not-exist' is not registered in the T1 model registry.",
    "details": {
      "model_id": "does-not-exist"
    }
  },
  "request_id": "<request_id>"
}
```

### `GET /models/nanonix-nano/availability`

Response `200`:

```json
{
  "model_id": "nanonix-nano",
  "available": true,
  "reason": null,
  "minimum_plan": "free",
  "request_id": "<request_id>"
}
```

### `GET /models/nanonix-nano/usage` *(authenticated)*

Response `200`:

```json
{
  "model_id": "nanonix-nano",
  "input_tokens_used": 0,
  "output_tokens_used": 0,
  "requests": 0,
  "input_token_limit": 8192,
  "output_token_limit": 2048,
  "input_remaining": 8192,
  "output_remaining": 2048,
  "is_exhausted": false,
  "request_id": "<request_id>"
}
```

### `POST /models/route` *(authenticated)*

Automatic routing. The plan comes from the key's assignment, not the request.

Request:

```json
{
  "input_tokens": 1200
}
```

Response `200`:

```json
{
  "model_id": "nanonix-mini-plus",
  "reason": "primary free-tier model",
  "cascade_position": 0,
  "policy_name": "free_tier_default",
  "considered": [
    {
      "model_id": "nanonix-mini-plus",
      "exhausted": false
    }
  ],
  "request_id": "<request_id>",
  "resolved_plan": "free"
}
```

### `POST /models/route` *(authenticated)*

Claiming a plan you do not hold is refused rather than honoured.

Request:

```json
{
  "plan": "enterprise",
  "input_tokens": 10
}
```

Response `403`:

```json
{
  "error": {
    "code": "AUTH_INSUFFICIENT_SCOPE",
    "message": "This key is assigned to plan 'free', not 'enterprise'. Plans are assigned server-side (POST /keys/assign); a client cannot select one.",
    "details": {
      "assigned_plan": "free"
    }
  },
  "request_id": "<request_id>"
}
```

## Usage and cost

### `GET /usage/current` *(authenticated)*

Response `200`:

```json
{
  "key_id": "<key_id>",
  "window_seconds": 86400.0,
  "current_window": {
    "requests": 1,
    "input_tokens": 12000,
    "output_tokens": 3400,
    "total_tokens": 15400
  },
  "all_time": {
    "requests": 1,
    "input_tokens": 12000,
    "output_tokens": 3400,
    "total_tokens": 15400
  },
  "by_model": [
    {
      "model_id": "nanonix-nano",
      "requests": 1,
      "input_tokens": 12000,
      "output_tokens": 3400,
      "total_tokens": 15400
    }
  ],
  "request_id": "<request_id>"
}
```

### `GET /usage/remaining` *(authenticated)*

Query:

```
{
  "model_id": "nanonix-nano"
}
```

Response `200`:

```json
{
  "model_id": "nanonix-nano",
  "input_remaining": 0,
  "output_remaining": 0,
  "is_exhausted": true,
  "request_id": "<request_id>"
}
```

### `GET /usage/history` *(authenticated)*

Query:

```
{
  "range": "24h",
  "limit": 5
}
```

Response `200`:

```json
{
  "events": [
    {
      "key_id": "<key_id>",
      "model_id": "nanonix-nano",
      "input_tokens": 12000,
      "output_tokens": 3400,
      "total_tokens": 15400,
      "requests": 1,
      "endpoint": "",
      "server_id": "<server_id>",
      "module_id": "",
      "user_id": "",
      "account_id": "",
      "ts": "<ts>"
    }
  ],
  "count": 1,
  "since": "<since>",
  "until": null,
  "limit": 5,
  "offset": 0,
  "request_id": "<request_id>"
}
```

### `GET /usage/by` *(authenticated)*

One endpoint for usage by model / key / server / module / user / account.

Query:

```
{
  "group_by": "model_id",
  "range": "24h"
}
```

Response `200`:

```json
{
  "group_by": "model_id",
  "rows": [
    {
      "group_by": "model_id",
      "value": "nanonix-nano",
      "requests": 1,
      "input_tokens": 12000,
      "output_tokens": 3400,
      "total_tokens": 15400
    }
  ],
  "count": 1,
  "since": "<since>",
  "until": null,
  "request_id": "<request_id>"
}
```

### `GET /usage/cost` *(authenticated)*

A forecast states the window it extrapolated from and how much to trust it.

Query:

```
{
  "range": "30d",
  "forecast": "true"
}
```

Response `200`:

```json
{
  "total_cost": 0.0,
  "currency": "USD",
  "since": "<since>",
  "until": null,
  "lines": [
    {
      "dimension": "model_id",
      "value": "nanonix-nano",
      "requests": 1,
      "input_tokens": 12000,
      "output_tokens": 3400,
      "total_tokens": 15400,
      "input_cost": 0.0,
      "output_cost": 0.0,
      "total_cost": 0.0,
      "currency": "USD"
    }
  ],
  "unpriced_models": [],
  "forecast": {
    "projected_cost": 0.0,
    "horizon_seconds": 2592000.0,
    "observed_seconds": "<observed_seconds>",
    "observed_cost": 0.0,
    "rate_per_day": 0.0,
    "confidence": "low",
    "currency": "USD",
    "basis": "<basis>"
  },
  "request_id": "<request_id>"
}
```

### `POST /usage/estimate` *(authenticated)*

Records nothing and reserves nothing. Omitting output_tokens gives an upper bound.

Request:

```json
{
  "model_id": "nanonix-nano",
  "input_tokens": 5000
}
```

Response `200`:

```json
{
  "model_id": "nanonix-nano",
  "requests": 1,
  "input_tokens": 5000,
  "output_tokens": 2048,
  "output_tokens_assumed": true,
  "input_cost": 0.0,
  "output_cost": 0.0,
  "estimated_cost": 0.0,
  "currency": "USD",
  "exceeds_input_token_limit": false,
  "exceeds_context_limit": false,
  "input_token_limit": 8192,
  "context_limit": 8192,
  "note": "Upper-bound estimate: output_tokens defaulted to the model's output_token_limit.",
  "request_id": "<request_id>"
}
```

## Keys

### `GET /keys` *(authenticated)*

A non-admin sees exactly one key: their own.

Response `200`:

```json
{
  "keys": [
    {
      "key_id": "<key_id>",
      "key_type": "user",
      "scopes": [
        "read",
        "write"
      ],
      "active": true,
      "created_at": "<created_at>",
      "expires_at": null,
      "prefix": "",
      "server_id": "<server_id>",
      "usage_count": 0,
      "request_count": 0,
      "usage_cap": null,
      "request_limit": null,
      "rotated_from": null,
      "assignment": null
    }
  ],
  "count": 1,
  "scope": "own",
  "request_id": "<request_id>"
}
```

### `POST /keys/assign` *(authenticated)*

Where a key's plan — and therefore its quota and fallback chain — comes from. Admin-only.

Request:

```json
{
  "key_id": "<key_id>",
  "plan": "free",
  "account_id": "acct-demo",
  "allowed_models": [
    "nanonix-nano"
  ]
}
```

Response `200`:

```json
{
  "assignment": {
    "key_id": "<key_id>",
    "plan": "free",
    "account_id": "acct-demo",
    "user_id": "",
    "server_ids": [],
    "allowed_models": [
      "nanonix-nano"
    ],
    "note": "",
    "assigned_at": "<assigned_at>"
  },
  "request_id": "<request_id>"
}
```

### `POST /keys/import` *(authenticated)*

Imports a Keymaster export. The response carries counts and masked ids, never key material.

Request:

```json
{
  "payload": {
    "keys": []
  }
}
```

Response `422`:

```json
{
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "Import payload contains no keys.",
    "details": {}
  },
  "request_id": "<request_id>"
}
```

## Servers

### `POST /servers/register` *(authenticated)*

Servers register UNTRUSTED. Only an admin can promote one, and only a trusted server can receive a module.

Request:

```json
{
  "name": "deploy-01",
  "address": "https://deploy-01.example.com"
}
```

Response `200`:

```json
{
  "server": {
    "server_id": "<server_id>",
    "name": "deploy-01",
    "address": "https://deploy-01.example.com",
    "trust_level": "untrusted",
    "status": "unknown",
    "capabilities": [],
    "tags": {},
    "registered_by": "ff712903-e346-47e8-8a96-f8305b4e0267",
    "created_at": "<created_at>",
    "updated_at": "<updated_at>",
    "last_seen": null
  },
  "request_id": "<request_id>"
}
```

### `GET /servers` *(authenticated)*

Response `200`:

```json
{
  "servers": [
    {
      "server_id": "<server_id>",
      "name": "deploy-01",
      "address": "https://deploy-01.example.com",
      "trust_level": "untrusted",
      "status": "unknown",
      "capabilities": [],
      "tags": {},
      "registered_by": "ff712903-e346-47e8-8a96-f8305b4e0267",
      "created_at": "<created_at>",
      "updated_at": "<updated_at>",
      "last_seen": null
    }
  ],
  "count": 1,
  "request_id": "<request_id>"
}
```

### `PATCH /servers/8a7a1a616de84b2f9fcdd9df1a11bb90` *(authenticated)*

Promotion is audited — it is the gate every module push checks.

Request:

```json
{
  "trust_level": "trusted"
}
```

Response `200`:

```json
{
  "server": {
    "server_id": "<server_id>",
    "name": "deploy-01",
    "address": "https://deploy-01.example.com",
    "trust_level": "trusted",
    "status": "unknown",
    "capabilities": [],
    "tags": {},
    "registered_by": "ff712903-e346-47e8-8a96-f8305b4e0267",
    "created_at": "<created_at>",
    "updated_at": "<updated_at>",
    "last_seen": null
  },
  "request_id": "<request_id>"
}
```

### `POST /servers/register` *(authenticated)*

The SSRF guard rejects the cloud-metadata endpoint unconditionally.

Request:

```json
{
  "name": "metadata",
  "address": "http://169.254.169.254/latest/meta-data"
}
```

Response `400`:

```json
{
  "error": {
    "code": "SSRF_BLOCKED",
    "message": "'169.254.169.254' is a cloud-metadata endpoint and is never allowed.",
    "details": {
      "address": "http://169.254.169.254/latest/meta-data"
    }
  },
  "request_id": "<request_id>"
}
```

### `DELETE /servers/8a7a1a616de84b2f9fcdd9df1a11bb90` *(authenticated)*

Destructive operations need ?confirm=true.

Response `409`:

```json
{
  "error": {
    "code": "CONFIRMATION_REQUIRED",
    "message": "Deleting a server registration is destructive and requires explicit confirmation. Re-send with ?confirm=true.",
    "details": {
      "action": "Deleting a server registration"
    }
  },
  "request_id": "<request_id>"
}
```

## Modules

### `POST /modules/create` *(authenticated)*

Request:

```json
{
  "name": "demo-module",
  "version": "1.0.0"
}
```

Response `200`:

```json
{
  "module": {
    "module_id": "<module_id>",
    "name": "demo-module",
    "version": "1.0.0",
    "owner_key_id": "7eda8aff-07e6-445b-bbf7-8e9e77e103dc",
    "status": "draft",
    "source_type": "none",
    "source_url": null,
    "checksum": null,
    "size_bytes": null,
    "deployed_servers": [],
    "metadata": {},
    "created_at": "<created_at>",
    "updated_at": "<updated_at>"
  },
  "request_id": "<request_id>"
}
```

### `GET /modules` *(authenticated)*

Response `200`:

```json
{
  "modules": [
    {
      "module_id": "<module_id>",
      "name": "demo-module",
      "version": "1.0.0",
      "owner_key_id": "7eda8aff-07e6-445b-bbf7-8e9e77e103dc",
      "status": "draft",
      "source_type": "none",
      "source_url": null,
      "checksum": null,
      "size_bytes": null,
      "deployed_servers": [],
      "metadata": {},
      "created_at": "<created_at>",
      "updated_at": "<updated_at>"
    }
  ],
  "count": 1,
  "request_id": "<request_id>"
}
```

### `POST /modules/upload/remote` *(authenticated)*

Registers and validates a source URL. It is NOT fetched here — POST /modules/{id}/fetch does that as a job.

Query:

```
{
  "module_id": "<module_id>"
}
```

Request:

```json
{
  "source_url": "https://cdn.example.com/demo-module-1.0.0.bin"
}
```

Response `429`:

```json
{
  "error": {
    "code": "RATE_LIMITED",
    "message": "Rate limit 'expensive_operations' exceeded for key. Retry in 5.8s.",
    "details": {
      "rule": "expensive_operations",
      "subject_kind": "key",
      "retry_after_seconds": 5.84,
      "cost": 10.0
    }
  },
  "request_id": "<request_id>"
}
```

### `POST /modules/fa2be64e77a84522bd2f8d9c37cd7aa8/deploy` *(authenticated)*

Multi-server deployment. Every target is trust-checked before the job is queued.

Request:

```json
{
  "server_ids": [
    "<SERVER_ID>"
  ]
}
```

Response `404`:

```json
{
  "error": {
    "code": "SERVER_NOT_FOUND",
    "message": "Server '<SERVER_ID>' is not registered.",
    "details": {
      "server_id": "<server_id>"
    }
  },
  "request_id": "<request_id>"
}
```

## Jobs and events

### `POST /jobs` *(authenticated)*

An unregistered job kind is NOT_SUPPORTED — a stable answer, not a 500.

Request:

```json
{
  "kind": "not-a-registered-kind",
  "payload": {}
}
```

Response `501`:

```json
{
  "error": {
    "code": "NOT_SUPPORTED",
    "message": "No job handler registered for kind 'not-a-registered-kind'.",
    "details": {
      "kind": "not-a-registered-kind",
      "available_kinds": [
        "module_fetch",
        "module_sync"
      ]
    }
  },
  "request_id": "<request_id>"
}
```

### `GET /events`

Polling form. GET /events/stream is the SSE live tail.

Query:

```
{
  "limit": 5
}
```

Response `200`:

```json
{
  "events": [
    {
      "event_id": "<event_id>",
      "type": "server.registered",
      "data": {
        "server_id": "<server_id>",
        "name": "deploy-01"
      },
      "source": "servers",
      "ts": "<ts>"
    },
    {
      "event_id": "<event_id>",
      "type": "server.updated",
      "data": {
        "server_id": "<server_id>",
        "trust_level": "trusted",
        "status": "unknown"
      },
      "source": "servers",
      "ts": "<ts>"
    },
    {
      "event_id": "<event_id>",
      "type": "module.created",
      "data": {
        "module_id": "<module_id>",
        "name": "demo-module"
      },
      "source": "modules",
      "ts": "<ts>"
    }
  ],
  "count": 3,
  "request_id": "<request_id>"
}
```

## Billing

### `GET /billing/balance` *(authenticated)*

Response `200`:

```json
{
  "account_type": "user",
  "account_id": "7eda8aff-07e6-445b-bbf7-8e9e77e103dc",
  "balance": 0.0,
  "request_id": "<request_id>"
}
```

### `POST /billing/payment-token` *(authenticated)*

Admin-only. The raw token is returned exactly once and stored only as a hash — capture it here or it is gone.

Request:

```json
{
  "amount": 25.0,
  "currency": "USD"
}
```

Response `200`:

```json
{
  "token": "<token>",
  "payment_token_id": "<payment_token_id>",
  "amount": 25.0,
  "currency": "USD",
  "request_id": "<request_id>"
}
```

### `POST /billing/redeem` *(authenticated)*

Redemption is single-use; a second attempt returns PAYMENT_TOKEN_ALREADY_REDEEMED.

Request:

```json
{
  "token": "<token>"
}
```

Response `400`:

```json
{
  "error": {
    "code": "PAYMENT_TOKEN_INVALID",
    "message": "Payment token is invalid.",
    "details": {}
  },
  "request_id": "<request_id>"
}
```

### `POST /billing/redeem` *(authenticated)*

The second attempt.

Request:

```json
{
  "token": "<token>"
}
```

Response `400`:

```json
{
  "error": {
    "code": "PAYMENT_TOKEN_INVALID",
    "message": "Payment token is invalid.",
    "details": {}
  },
  "request_id": "<request_id>"
}
```

### `GET /billing/transactions` *(authenticated)*

Transaction and payment-token ids are masked in every response.

Response `200`:

```json
{
  "transactions": [
    {
      "transaction_id": "<transaction_id>",
      "account_type": "user",
      "account_id": "7eda8aff-07e6-445b-bbf7-8e9e77e103dc",
      "amount": 25.0,
      "kind": "redeem",
      "balance_after": 25.0,
      "note": "payment token redemption",
      "created_by": "7eda8aff-07e6-445b-bbf7-8e9e77e103dc",
      "created_at": "<created_at>"
    }
  ],
  "count": 1,
  "request_id": "<request_id>"
}
```

## Security and audit

### `GET /security/network` *(authenticated)*

Response `200`:

```json
{
  "entries": [],
  "count": 0,
  "allow_unlisted_clients": true,
  "enabled": true,
  "request_id": "<request_id>"
}
```

### `POST /security/network/blacklist` *(authenticated)*

Request:

```json
{
  "cidr": "203.0.113.0/24",
  "reason": "abuse"
}
```

Response `200`:

```json
{
  "entries": [
    {
      "entry_id": "<entry_id>",
      "cidr": "203.0.113.0/24",
      "kind": "block",
      "reason": "abuse",
      "created_by": "ff712903\u2026",
      "created_at": "<created_at>",
      "expires_at": null,
      "expired": false
    }
  ],
  "count": 1,
  "allow_unlisted_clients": true,
  "enabled": true,
  "request_id": "<request_id>"
}
```

### `POST /security/network/whitelist` *(authenticated)*

Request:

```json
{
  "cidr": "100.64.0.0/10",
  "reason": "tailnet"
}
```

Response `200`:

```json
{
  "entries": [
    {
      "entry_id": "<entry_id>",
      "cidr": "100.64.0.0/10",
      "kind": "allow",
      "reason": "tailnet",
      "created_by": "ff712903\u2026",
      "created_at": "<created_at>",
      "expires_at": null,
      "expired": false
    },
    {
      "entry_id": "<entry_id>",
      "cidr": "203.0.113.0/24",
      "kind": "block",
      "reason": "abuse",
      "created_by": "ff712903\u2026",
      "created_at": "<created_at>",
      "expires_at": null,
      "expired": false
    }
  ],
  "count": 2,
  "allow_unlisted_clients": true,
  "enabled": true,
  "request_id": "<request_id>"
}
```

### `POST /security/limits` *(authenticated)*

waiter -r. A forced limit only ever tightens — it applies in addition to the configured rules.

Request:

```json
{
  "subject_type": "key",
  "subject_id": "<KEY_ID>",
  "requests_per_window": 60,
  "window_seconds": 60,
  "reason": "noisy client"
}
```

Response `200`:

```json
{
  "limit": {
    "subject_type": "key",
    "subject_id": "<KEY_ID>",
    "requests_per_window": 60,
    "tokens_per_window": null,
    "window_seconds": 60.0,
    "reason": "noisy client",
    "created_at": "<created_at>"
  },
  "request_id": "<request_id>"
}
```

### `GET /security/rate-limits` *(authenticated)*

Your own remaining budget, so you can back off before a 429 rather than after one.

Response `200`:

```json
{
  "rules": [
    {
      "rule": "per_key_default",
      "capacity": 120,
      "remaining": 120,
      "refill_per_second": 2.0
    },
    {
      "rule": "expensive_operations",
      "capacity": 20,
      "remaining": 20,
      "refill_per_second": 0.333
    }
  ],
  "enabled": true,
  "request_id": "<request_id>"
}
```

### `GET /audit` *(authenticated)*

Admin-only, and reading it is itself audited.

Query:

```
{
  "limit": 3
}
```

Response `200`:

```json
{
  "events": [
    {
      "audit_id": "<audit_id>",
      "ts": "<ts>",
      "category": "admin",
      "action": "audit.read",
      "outcome": "success",
      "actor_key_id": "<actor_key_id>",
      "actor_is_admin": true,
      "resource_type": "audit",
      "resource_id": "",
      "client_ip": "127.0.0.1",
      "request_id": "<request_id>",
      "details": {
        "category": null,
        "action": null,
        "limit": 3
      }
    },
    {
      "audit_id": "<audit_id>",
      "ts": "<ts>",
      "category": "admin",
      "action": "limits.force",
      "outcome": "success",
      "actor_key_id": "<actor_key_id>",
      "actor_is_admin": true,
      "resource_type": "key",
      "resource_id": "<resource_id>",
      "client_ip": "127.0.0.1",
      "request_id": "<request_id>",
      "details": {
        "requests_per_window": 60,
        "tokens_per_window": "[redacted]",
        "window_seconds": 60.0,
        "reason": "noisy client"
      }
    },
    {
      "audit_id": "<audit_id>",
      "ts": "<ts>",
      "category": "security",
      "action": "network.whitelist.add",
      "outcome": "success",
      "actor_key_id": "<actor_key_id>",
      "actor_is_admin": true,
      "resource_type": "network_policy",
      "resource_id": "<resource_id>",
      "client_ip": "127.0.0.1",
      "request_id": "<request_id>",
      "details": {
        "reason": "tailnet",
        "expires_at": null
      }
    }
  ],
  "count": 3,
  "total": 14,
  "limit": 3,
  "offset": 0,
  "request_id": "<request_id>"
}
```

### `GET /audit` *(authenticated)*

A non-admin is refused — the audit log is a map of who administers this deployment.

Response `403`:

```json
{
  "error": {
    "code": "AUTH_ADMIN_REQUIRED",
    "message": "This operation requires an admin-scoped T1 key.",
    "details": {}
  },
  "request_id": "<request_id>"
}
```
